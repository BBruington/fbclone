
export default function Post({name, message, email, postImage, image, timestamp}) {
  console.log('name', name)
  return (
    <div>
      <div>{name}</div>
      <div>name</div>
    </div>
    
  )
}